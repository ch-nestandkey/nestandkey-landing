# Moved

This repo is retired (merged into `nest-key-app`, 2026-07-26). `CLAUDE.md` and
`DESIGN_HANDOFF.md` now live in `nest-key-app` — that's the current, maintained
copy. This repo is kept only as the pre-merge rollback reference; nothing
should be built here.
